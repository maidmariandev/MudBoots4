package Graph.Player.PlayerTypes

import Graph.GraphIdentity
import Graph.Player.Player
import Graph.Zone.World

open class AIPlayer(override val who: GraphIdentity) : Player(who) {

    fun takeTurn(world: World) {

    }
}

